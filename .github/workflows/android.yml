name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Install Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies and update capacitor
      working-directory: ./android
      run: |
        npm install
        npx @capacitor/cli update android || npx cap update android || ./node_modules/.bin/cap update android

    - name: Grant execute permission for gradlew
      working-directory: ./android
      run: chmod +x gradlew
      
    - name: Build with Gradle
      working-directory: ./android
      run: ./gradlew build
